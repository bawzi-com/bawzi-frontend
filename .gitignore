node_modules
.next
out
dist
.env
.env.local
.DS_Store
# Mesma limpeza do backend: backup versionado e cache de compilação do
# TypeScript não pertencem ao repositório.
_backup_pre_fix/
_to_delete/
archive/
*.tsbuildinfo
*.db

# Artefato incremental do TypeScript — muda a cada build e gera conflito
# em todo merge. Não é código nem configuração.
tsconfig.tsbuildinfo
COMMIT_MSG.tmp
